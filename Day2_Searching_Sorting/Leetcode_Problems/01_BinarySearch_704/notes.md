### Approach
Use two pointers `left` and `right`. Check the middle element and eliminate
half of the search space based on comparison with the target.

Binary Search works when the search space is sorted.
